# 텍스트 파일은 저장소에 LF 로 저장하고, 체크아웃 시 각 OS 에 맞춰 준다.
* text=auto

# 아래는 절대 줄바꿈 변환을 하면 안 되는 파일들 (변환하면 파일이 깨진다).
*.pt   binary
*.pth  binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.xlsx binary
